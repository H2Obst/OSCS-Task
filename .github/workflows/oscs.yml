name: oscs
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      buildDir: '${{github.workspace}}/build'
    steps:
    - name: echo
      run: echo "$buildDir"
    - name: mkdir
      run: mkdir build
    - name: build
      run: cmake --build build -DCMAKE_BUILD_TYPE=Debug --target all
